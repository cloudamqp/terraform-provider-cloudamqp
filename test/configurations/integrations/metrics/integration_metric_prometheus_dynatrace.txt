resource "cloudamqp_integration_metric_prometheus" "dynatrace" {
  instance_id = {{.InstanceID}}
  dynatrace {
    environment_id = "{{.DynatraceEnvironmentID}}"
    access_token   = "{{.DynatraceAccessToken}}"
    tags          = "{{.DynatraceTags}}"
  }
}
